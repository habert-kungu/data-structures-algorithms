// // classic version
// function secondLargest(arr) {
//   if (arr.length < 2) return -1;
//   let sortedArray = [...arr]
//     .filter((item, index, self) => self.indexOf(item) === index)
//     .sort((a, b) => b - a);
//   console.log(sortedArray[1] ?? -1)
// }
//
let arr = [2, 3, 4, 6, 1, 1, 3];
function secondLargest(arr) {
  let largest = -Infinity
  let secondLargestVar = -Infinity
  for (const number of arr){
    if (let number > largest ){
      ;wqa
      k

    }
  }

}

secondLargest(arr);
